model locations {
    id Int @id @default(autoincrement())

    name String
    address String
    
    theaters theaters[]

    created_at DateTime @default(now())
    updated_at DateTime @updatedAt
}