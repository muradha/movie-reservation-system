model users {
    id Int @id @default(autoincrement())

    name String
    email String @unique
    password String
    role_user role_user[]

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}